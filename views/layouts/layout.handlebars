<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://fonts.googleapis.com/css?family=Rubik:400,700,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/trix.css">
    <link rel="stylesheet" href="/css/app.css">
    <title>{{nombrePagina}}</title>
</head>
<body>

    <div class="nombre-sitio contenedor">
        <h1>
            <a href="/">devJobs</a>
        </h1>
        {{#if barra}}
            <div class="buscador">
                <form action="/buscador" method="post">
                    <input type="text" name="q" class="buscar" placeholder="Buscar vacante...">
                    <input type="submit" value="Buscar">
                </form>
            </div>
        {{/if}}

        {{#if cerrarSesion}}
            <div class="cerrar-sesion">
                <p>Hola! {{usuario}}</p>
                {{#if imagen}}
                    <img src="/uploads/perfiles/{{imagen}}" alt="Profile">
                {{/if}}
                <a href="/administracion">Administrar</a>
                <a href="/cerrar-sesion">Cerrar sesion</a>
            </div>
        {{/if}}
    </div>

    <header class="site-header contenedor separador">
        <h2>{{nombrePagina}}</h2>
        
        {{#if tagLine }}
            <p class="tagline">{{tagLine}}</p>
        {{/if}}

        {{#if boton}}
            <a href="/vacantes/nueva" class="btn btn-azul">Publicar nueva vacante</a>
        {{/if}}
    </header>

    <div class="contenedor-principal contenedor">
        {{#if mensajes}}
            <div class="alertas">
                {{#mostrarAlertas mensajes}}
                {{/mostrarAlertas}}
            </div>
        {{/if}}
        {{!-- Triple llaves para imprimir HTML con handlebars --}}
        {{{body}}}
    </div>
    
    <script src="/dist/bundle.js"></script>
    <script src="/js/trix.js"></script>
</body>
</html>