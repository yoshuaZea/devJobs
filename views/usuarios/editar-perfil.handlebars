<main class="contenedor">
    <form action="/editar-perfil" method="post" class="default-form" enctype="multipart/form-data">
        <div class="campo">
            <label for="text">Nombre</label>
            <input type="text" name="nombre" value="{{datos_usr.nombre}}" placeholder="Tu nombre">
        </div>
        <div class="campo">
            <label for="text">Correo electrónico</label>
            <input type="email" name="email" value="{{datos_usr.email}}" placeholder="Tu correo electrónico">
        </div>
        <div class="campo">
            <label for="text">Contraseña</label>
            <input type="password" name="password" placeholder="Tu contraseña">
        </div>
        <div class="campo">
            <label for="text">Imagen</label>
            <input type="file" name="imagen" placeholder="Tu imagen">
            <img src="/uploads/perfiles/{{datos_usr.imagen}}" class="admin-perfil" alt="Profile IMG">
        </div>
        <div class="campo">
            <input type="submit" class="btn btn-azul" value="Guardar cambios">
        </div>
    </form>
</main>